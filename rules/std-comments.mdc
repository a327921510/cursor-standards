---
description: 代码注释规范——AI 生成须写清职责与流程地图（stage），详细且无废话，降低人类阅读成本
alwaysApply: true
---

# 代码注释

项目代码大量由 AI 生成，编写 / 修改代码时**主动补注释**，为人类阅读与后续迭代降本。注释**正文用中文**，技术术语（API 名、协议字段、错误码等）**保留英文**。

## 必须注释（写「为什么」，而非「做了什么」）
- **文件头**：非平凡模块用一两句说明职责、在链路中的位置（如「请求封装」「回调处理」）。
- **编排入口的流程地图**：用户或调用方可见的主路径入口须在文件头或入口函数头列出 **stage 1→N**（短英文名 + 一句中文）；与 `std-readability` 的「主流程可扫读」一致。
- **函数 / 流程块**：业务目的、关键输入输出、副作用；跨层 / 跨端调用写清**数据流向**。
- **非显而易见处**：协议字段、约定的错误码 / 状态码、降级路径、平台限制、magic number —— 注释**为什么**。
- **复杂分支**：重试、降级、边界与错误处理，用简短行内或块注释说明意图。

## 详细程度门槛（避免漏注 / 过度注）
- **公共 / 导出函数**：必须有函数头（职责 + 非自明的参数 / 返回 / 副作用）。
- **私有小函数、命名已自明者**：可省文件头 / 函数头，仅在非显而易见处注释。
- **含非平凡分支或超过约 30 行的函数**：至少用块注释标出主要步骤。
- **主流程函数**：用 `// --- stageName ---` 分段，**stageName 与流程地图同一套**；不要用「解析入参」「处理数据」这类无法搜索的泛称。

## 流程地图示例

```javascript
/**
 * 提交导出任务（编排入口）
 * 位置：调用方确认参数后 → 创建任务并等待结果
 *
 * 1. validateInput   校验必填与权限
 * 2. createJob       创建远端任务
 * 3. awaitResult     轮询或订阅直至完成/失败
 * 4. resolveOutcome  映射结果码并返回
 */
async function submitExport(params) {
  // --- validateInput ---
  // ...
  // --- createJob ---
  // ...
}
```

关键节点日志（若项目已有日志）带同一 `stage` 字段，便于搜 `createJob` 落到代码块。日后 E2E / 集成测试的定位属性前缀也用该名。不要求为每个 stage 新建函数。

## 禁止废话注释（无废话红线）
- 不注释变量自明的代码：`const user = getUser()` 不必写「获取用户」。
- 不复述代码字面：紧邻 `foo.scan()` 写 `// 调用 scan` —— 禁止。
- 不用注释替代命名：命名能表达的**先改命名**，再考虑注释。
- 不留过时注释：改逻辑时**同步更新 / 删除**相关注释（AI 尤其易残留过时注释）。
- 不保留被注释掉的死代码（dead code）；`TODO` 需附明确说明或追踪。

## 反例 vs 正例

```javascript
// ❌ 废话：复述代码字面
// 获取用户
const user = getUser();

// ✅ 说明与服务端约定的非显而易见含义
// code 约定：1=成功 / -1=失败 / 0=用户取消
if (res.code === 0) return;
```

```javascript
// ✅ 文件头：说明职责与在链路中的位置
/**
 * 外部回调处理：校验签名 → 落库 → 通知下游；失败进重试队列
 */
```
